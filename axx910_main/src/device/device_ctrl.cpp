#include "device_ctrl.h"
#include "device_ctrl_cfg.h"
#include "print_common.h"

void InitDevices(DEVICE_LIST* device_list)
{
    //InitRadar(&(device_list->radar_device));
    //InitCamera(&(device_list->camera_device));
}

void GetDataFromDevices(DEVICE_LIST* device_list, MultiPRF_t curPRF)
{
    //CaptureRadar(&device_list->radar_device);
    //CaptureCamera(&device_list->camera_device);
}